const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch({headless: true}); const page = await browser.newPage(); await page.goto('https://www.rndtechnosoft.com/blogs', {waitUntil: 'networkidle2'}); const html = await page.content(); const cheerio = require('cheerio'); const $ = cheerio.load(html); console.log('H1:', $('h1').length, 'H2:', $('h2').length); await browser.close(); })();
